# 常驻待办（Standing Todos）

> 这是 daily-focus 的常驻任务池。每次运行 `/daily-focus` 时本文件会被读取。
> 写法：每条用 `- [ ] 任务描述`，完成改成 `- [x]`；留空也没关系，skill 会自动跳过。
> 可选状态标记（`[someday:YYYY-MM-DD]`、`[doing]`、`[delegated:名字]`）追加在任务描述末尾，含义与建池规则见 SKILL.md 步骤 3B。

- [ ] 每周做一次项目复盘
- [ ] 小榄北冰杯落地
