# ADR 0023: Remove gateway.py re-export layer

- **Status**: Deprecated
- **Reason**: Does not meet ADR门槛 (not hard to reverse, not surprising, no real trade-off). This was a routine cleanup — deleting a zero-caller re-export module — not an architectural decision. Recorded here only because the number is already allocated.
